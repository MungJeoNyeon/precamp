// cash.js

export class CashService {
    checkValue = () => {
        // 1. 가진돈 검증하는 코드 (대략 10줄 정도)
        // ...
        // ...
        // ...
        // ...
    }
}

// `CashService` 라는 class를 만들었습니다.

// - class 안에 가진돈을 검증하는 코드를`checkValue()` 라는 객체 메서드에 만들어 줍니다.
// - class 앞에 `export` 를 붙여주어서 외부에서 불러올 수 있도록 설정해 줍니다.